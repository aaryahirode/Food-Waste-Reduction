<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donor Dashboard | MealMatch</title>
    <link rel="stylesheet" href="styles/recepeint_home.css">
</head>
<body>
    <nav>
        <div class="nav-content">
            <div class="logo">MealMatch</div>
            <div class="profile-menu">
                <%if(locals.id){%>
                    <div class="profile-icon"><a href="/donor-profile?id=<%= id %>">👤</a></div>
                <%}else{%>
                    <div class="profile-icon"><a href="/donor-profile">👤</a></div>
                <%}%>
            </div>
        </div>
    </nav>
    
    <div class="container">
        <!-- Sidebar -->
        <div class="sidebar">
            <ul>
                <%if(locals.id){%>
                    <li><a href="/recipient?id=<%=id  %>">Home</a></li>
                    <li><a href="/recipient-request?id=<%=id  %>">Accepted Requests</a></li>
                <%}else{%>
                    <li><a href="/recipient">Home</a></li>
                    <li><a href="/recipient-request">Accepted Requests</a></li>
                <%}%>
                
                
            </ul>
        </div>

        <!-- Main Content -->
        <div class="main-content">
            <div class="home-section">
                <h2 class="tc">Welcome to MealMatch</h2>
                <p>Access nutritious meals and support sustainability.</p>
                <%if(locals.id){%>
                    <a href="/recipient-form?id=<%=id  %>" class="action-button">Explore Donors</a>
                <%}else{%>
                    <a href="/recipient-form" class="action-button">Explore Donors</a>
                <%}%>
            </div>
        </div>
    </div>
    <!-- Google Translate Script -->
    <script type="text/javascript">
        function googleTranslateElementInit() {
        new google.translate.TranslateElement(
            { pageLanguage: 'en' },
            'google_translate_widget'
        );
        }
    </script>
    <script type="text/javascript" 
        src="https://translate.google.com/translate_a/element.js?cb=googleTranslateElementInit">
    </script>
</body>
</html>